{
  "name": "image-watermark-tool",
  "version": "1.0.0",
  "description": "圖片浮水印工具 - 靜態網頁應用程式",
  "main": "index.html",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:verbose": "jest --verbose",
    "build": "echo 'Files already in docs folder - ready for GitHub Pages'",
    "serve": "cd docs && python -m http.server 8000",
    "serve:node": "npx http-server docs -p 8000 -o",
    "deploy:check": "echo 'Checking docs folder...' && ls -la docs/",
    "deploy:help": "echo 'To deploy: 1) git add . 2) git commit -m \"Deploy to GitHub Pages\" 3) git push origin main 4) Enable GitHub Pages in repo settings'",
    "sync": "node sync-to-docs.js",
    "deploy": "npm run sync && git add docs/ && git commit -m 'Update GitHub Pages deployment' && git push origin main"
  },
  "keywords": [
    "watermark",
    "image",
    "javascript",
    "canvas",
    "bootstrap"
  ],
  "author": "Image Watermark Tool",
  "license": "MIT",
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "setupFilesAfterEnv": ["<rootDir>/js/test-setup.js"],
    "collectCoverageFrom": [
      "js/app.js",
      "!js/app.test.js",
      "!js/test-setup.js"
    ],
    "coverageReporters": [
      "text",
      "lcov",
      "html"
    ],
    "testMatch": [
      "**/*.test.js"
    ],
    "verbose": true
  }
}